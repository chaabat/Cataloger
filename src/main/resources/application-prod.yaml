spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/cataloger
    username: chaabat
    password: nothing
    driver-class-name: org.mariadb.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true